'use strict'

const BumblebeeTransformer = use('Bumblebee/Transformer')

/**
 * SalonServiceTransformer class
 *
 * @class SalonServiceTransformer
 * @constructor
 */
class SalonServiceTransformer extends BumblebeeTransformer {
  /**
   * This method is used to transform the data.
   */
  transform (model) {
    return model
  }
}

module.exports = SalonServiceTransformer
